---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: chaosdemo-db
    release: primary
  name: chaosdemo-db
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
